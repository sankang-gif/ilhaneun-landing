# 일하는학교 랜딩페이지 디자인 가이드
### 참고: 사단법인 점프 (jumpsp.org) — 2026-09-05 실측 기준

> **1~4장은 점프 실측치**(브라우저에서 `getComputedStyle`로 직접 추출),
> **5~8장은 일하는학교 적용안**입니다.
> 적용 대상 파일: [index.html](index.html)

> **개정 이력**
> - 2026-09-05 초판 — 팔레트를 딥그린(#14563F) 기준으로 작성
> - 2026-09-05 **개정** — 일하는학교 로고 실제 색(하늘색 `#68C9F3` / 검정 `#111315`)으로 팔레트 전면 교체.
>   초판의 그린 계열은 근거 없는 임의 선택이었으므로 폐기.

---

## 0. 요약 — 점프에서 가져올 것 / 가져오지 않을 것

| | 항목 | 판단 |
| --- | --- | --- |
| ✅ | **Pretendard 폰트** | 국문 비영리 웹의 사실상 표준 |
| ✅ | **알약형(50px) CTA 버튼, 20px 54px 패딩** | 넉넉하고 누르고 싶게 생김 |
| ✅ | **CTA 색 분리 전략** (후원=단독 강조색, 나머지=중립/브랜드색) | 후원 유치 페이지의 핵심 |
| ✅ | **한글 제목 + 영문 서브라벨 + 세로 구분선** 섹션 헤더 | 정돈된 비영리 기관 톤 |
| ✅ | **극단적 폰트웨이트 대비** (100 ↔ 600/700) | 사진 위 타이틀에서 특히 효과적 |
| ✅ | **풀블리드 사진 밴드 + 그 위에 떠 있는 흰 카드** | 후원 신청 위젯 배치에 차용 |
| ✅ | **좌측 SNS 레일 / 우측 퀵메뉴 플로팅** | 후원 상시 노출 장치로 응용 |
| ⚠️ | **1480px 컨테이너** | 1280px로만 확장 (사진 없이 1480은 허전함) |
| ⚠️ | **시안 계열 브랜드색** | 점프 `#16B9ED`와 일하는학교 로고 `#68C9F3`이 **같은 계열**. 차별화 장치 필요 → 5-3 참조 |
| ❌ | **레드(#F23838) 후원 CTA** | 점프의 색. 일하는학교는 버밀리언(`#CE4520`) 사용 |
| ❌ | **fullPage.js 스크롤 하이재킹** | 후원 전환에 불리(정보 탐색이 느려짐). 일반 스크롤 유지 |
| ❌ | **본문 14px** | 국문 가독성 위해 16px 유지 |

---

## 1. 점프 실측 — 컬러 토큰

| 역할 | HEX | 사용처 |
| --- | --- | --- |
| 배경 (지배색) | `#FFFFFF` | 페이지 대부분. 흰 여백이 기본값 |
| 브랜드 프라이머리 | `#16B9ED` | 로고, 헤더 CTA, 강조 텍스트, 탭 활성, 아이콘 |
| 후원 CTA 전용 | `#F23838` | **오직 '후원하기' 버튼 하나에만** 사용 |
| 다크 서피스 | `#252525` / `#333333` | 다크 섹션 배경, 보조 버튼 |
| 라이트 서피스 | `#F4F5F8` | 카드/섹션 구분용 아주 옅은 회색 |
| 본문 텍스트 | `#676767` | 설명문 전부. 검정이 아님 |
| 제목 텍스트 | `#000000` | 헤드라인만 순수 검정 |
| 보조/비활성 | `#CCCCCC`, `#999999` | 구분선, 캡션 |

**관찰 포인트 — 색을 아낀다.**
페이지 면적의 90% 이상이 흰색이고, 유채색은 딱 두 개(시안·레드)만 씁니다.
시안은 '브랜드', 레드는 '행동(후원)'으로 역할이 완전히 분리되어 있어서
**빨간 버튼이 화면에 나타나면 그것 하나가 시선을 독점**합니다.

---

## 2. 점프 실측 — 타이포그래피

**폰트**: `"Pretendard Variable", Pretendard, -apple-system, ... , "Apple SD Gothic Neo", "Noto Sans KR", sans-serif`

| 요소 | size | weight | line-height | letter-spacing |
| --- | --- | --- | --- | --- |
| 사진 위 페이지 타이틀 | 55px | **100** (Thin) | 61px | normal |
| 섹션 헤드라인 | 46px | 700 | 60px | **-1px** |
| 카드 제목 (H3) | 22px | 600 | 34px | normal |
| 통계 숫자 (대) | 60px | 600 | 60px | normal |
| 통계 숫자 (소) | 33px | 600 | 33px | normal |
| 내비게이션 | 17px | 500 | 25px | -0.5px |
| 버튼 | 17px | 600 | — | — |
| 본문 | 14px | 400 | 26px | -0.5px |

**관찰 포인트 두 가지.**
1. **웨이트 100과 700을 나란히 쓴다.** 사진 위 타이틀은 Thin(100), 본문 헤드라인은 Bold(700).
   중간 웨이트를 어정쩡하게 쓰지 않아서 위계가 선명합니다.
2. **큰 글자에만 음수 자간(-1px).** 국문 큰 제목은 자간을 좁혀야 덩어리로 읽힙니다.
   반대로 60px 통계 숫자는 자간 normal — 숫자는 좁히지 않습니다.

---

## 3. 점프 실측 — 컴포넌트

### 3-1. 버튼 (전부 동일 규격)

```
padding      : 20px 54px
border-radius: 50px          /* 완전한 알약형 */
font         : 17px / 600
border       : none
```

| 변형 | 배경 | 글자 |
| --- | --- | --- |
| 후원 (최상위) | `#F23838` | `#FFFFFF` |
| 브랜드 | `#16B9ED` | `#FFFFFF` |
| 중립 | `#333333` | `#FFFFFF` |
| 다크 | `#000000` | `#FFFFFF` |

→ **테두리 버튼(ghost)이 아예 없습니다.** 모든 버튼이 채워진 솔리드.

### 3-2. 레이아웃

```
container max-width : 1480px
header height       : 98px   (배경 투명, 스크롤 시 흰 배경)
```

### 3-3. 페이지 헤더 패턴 (모든 하위 페이지 공통)

```
[ 풀블리드 사진 + 어두운 오버레이 ]
        영문 대문자 라벨 (55px / weight 100 / 흰색)
                    ↓
        한글 제목 (예: 후원하기)
        영문 한 줄 (Join Us to Maximize our Potential)
                    ↓
              │        ← 얇은 세로 구분선 1개
                    ↓
        "따옴표로 감싼 큰 문장"
```

세로 선 하나로 구획을 나누는 게 이 사이트의 시그니처 장식입니다.

### 3-4. 사진 위에 떠 있는 흰 카드

후원 페이지·성과 페이지 모두 같은 구조:
풀블리드 사진 밴드 위에 흰 카드가 걸쳐서 올라가고, 카드 안에 **탭(정기후원/일시후원)** → 폼 → 버튼.
**후원 행동을 사진의 감정 위에 물리적으로 얹는** 배치입니다.

### 3-5. 통계 진열

5개 셀을 가로로 나열하고 셀 사이에 얇은 세로 구분선.
각 셀 = 라인 아이콘(단색) + 60px 숫자 + 작은 설명.
기준일("2025년 12월 기준")을 반드시 병기합니다.

### 3-6. 플로팅 요소

- **좌측 하단 레일**: 블로그·인스타·유튜브 원형 아이콘 세로 배치
- **우측 하단**: 퀵메뉴 2개 (아이콘 + 라벨)
- **우측 중앙**: 섹션 인디케이터 점

---

## 4. 점프 실측 — 인터랙션

- **fullPage.js** 기반. 한 번 스크롤 = 한 화면 전환.
- 히어로는 3장짜리 슬라이더 (하단 점 인디케이터).
- 섹션 진입 시 텍스트가 opacity 0 → 1 로 페이드인.
- 배경에 파스텔 블롭/도형이 아주 옅게 깔림 (핑크·민트·연블루).

> **일하는학교에는 스크롤 하이재킹을 적용하지 않기를 권합니다.**
> 후원 전환은 "훑어보고 → 납득하고 → 누르는" 흐름인데, 한 화면씩 끊기면 정보 탐색 속도가 떨어집니다.
> 대신 **페이드인 등장 효과**(이미 구현됨)와 **파스텔 배경 도형**만 가져옵니다.

---

## 5. 일하는학교 적용 — 컬러 결정

### 5-1. 근거 — 로고 실제 색

일하는학교 로고에서 추출된 색은 다음 두 가지입니다.

```
하늘색  #68C9F3
검정    #111315
```

브랜드색을 임의로 정하지 않고 **이 두 색을 기준점으로 삼습니다.**
다만 `#68C9F3`은 밝아서 흰 배경 위 글자로 쓰면 읽히지 않으므로(대비 1.9:1),
텍스트·버튼용 진한 변형을 파생시킵니다.

### 5-2. 확정 팔레트

| 역할 | 토큰 | HEX | 흰 배경 대비 | 용도 |
| --- | --- | --- | --- | --- |
| 페이지 배경 | `--bg` | `#FFFFFF` | — | 기본 배경 |
| 서피스 | `--surface` | `#F5F7F9` | — | 섹션 교대 구분 |
| **로고 하늘색** | `--brand` | `#68C9F3` | 1.9:1 | 도형·밑줄·불릿·그래프. **글자 금지** |
| 브랜드 딥 | `--brand-deep` | `#1595CC` | 3.4:1 | 버튼 배경, 아이콘, 24px 이상 큰 숫자 |
| 브랜드 잉크 | `--brand-ink` | `#0E6E99` | **5.7:1** | 작은 강조 텍스트·링크·kicker (AA 통과) |
| 브랜드 틴트 | `--brand-tint` | `#EAF7FE` | — | 배지·칩·카드 헤더 배경 |
| **후원 CTA 전용** | `--donate` | `#CE4520` | **4.8:1** | 후원 버튼에만 |
| 후원 hover | `--donate-hover` | `#B33A19` | — | |
| 다크 서피스 | `--dark` | `#111315` | — | 다크 섹션 배경 (= 로고 검정) |
| 제목 텍스트 | `--ink` | `#111315` | 17.9:1 | 헤드라인 (= 로고 검정) |
| 본문 텍스트 | `--ink-2` | `#4A5560` | 7.5:1 | 설명문 |
| 캡션/보조 | `--muted` | `#6B7681` | 4.7:1 | 출처·주석 (AA 통과) |
| 구분선 | `--line` | `#E4E9ED` | — | 카드 테두리·셀 구분 |
| 구분선(옅음) | `--line-soft` | `#EEF2F5` | — | 내부 구분 |

**대비값은 모두 실제 계산치입니다.** `--brand-ink`, `--muted`, `--donate`(흰 글자) 모두 WCAG AA(4.5:1)를 넘깁니다.
`--brand-deep`(3.4:1)은 **24px 이상 또는 18.5px Bold 이상에서만** 텍스트로 사용하세요.

### 5-3. 점프와 겹치는 문제, 그리고 차별화

일하는학교 로고 하늘색(`#68C9F3`)은 점프 브랜드색(`#16B9ED`)과 거의 같은 계열입니다.
같은 색을 쓰면서 다르게 보이려면 **채도가 아니라 구조로 구별**해야 합니다.

| 축 | 점프 | 일하는학교 |
| --- | --- | --- |
| 무채축 | 순수 검정 `#000000` | **로고 검정 `#111315`** (아주 살짝 푸른 기) |
| 행동색 | 레드 `#F23838` | **버밀리언 `#CE4520`** (붉은 주황 — 따뜻하고 덜 경고적) |
| 하늘색 역할 | 텍스트·버튼에 광범위 사용 | **면·도형·밑줄 위주**, 글자는 딥/잉크 변형만 |
| 다크 섹션 | 회색 `#252525` | **로고 검정 `#111315`** |

버밀리언을 고른 이유: 하늘색의 보색 방향이면서, 레드보다 따뜻해서
"경고"가 아니라 "온기"로 읽힙니다. 청년 자립이라는 주제에 레드는 과합니다.

### 5-4. 색 사용 규칙 (반드시 지킬 것)

1. **`--donate`(#CE4520)는 후원 버튼에만.** 배지·아이콘·밑줄·그래프 어디에도 쓰지 않습니다.
   페이지 전체에서 이 색이 나타나는 지점은 **3~4곳**(헤더 CTA, 히어로 CTA, 참여 섹션 CTA, 하단 고정 FAB)뿐이어야 합니다.
2. **기존 앰버(`#E0913A`)는 전면 폐기.** kicker·밑줄·아이콘·그래프에 두루 쓰여 강조가 분산돼 있었습니다.
   → 장식은 `--brand`, 텍스트는 `--brand-ink`로 흡수.
3. **기존 그린(`#14563F`) 계열 전면 폐기.** 로고 근거가 없습니다.
4. 유채색은 **하늘색 계열 + 버밀리언, 총 2계열**까지.
5. 그래프/차트는 하늘색 명도 단계로만:
   `#1595CC` → `#68C9F3` → `#A8DDF6` → `#CBD3DA`(무채)

---

## 6. 일하는학교 적용 — 타이포그래피

### 6-1. 폰트 교체

```html
<link rel="stylesheet"
      href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable-dynamic-subset.min.css">
```
```css
--font:"Pretendard Variable",Pretendard,-apple-system,BlinkMacSystemFont,
       system-ui,"Apple SD Gothic Neo","Noto Sans KR","Malgun Gothic",sans-serif;
```

> 같은 폴더의 Apps Script 프로젝트(`Index.html`)가 이미 이 CDN을 쓰고 있어 **두 프로젝트의 폰트가 일치**합니다.
> Artifact로 배포할 경우에만 `cdn.jsdelivr.net/npm/` 경로 패키지로 바꾸면 됩니다.

### 6-2. 타입 스케일 (점프 비율 + 국문 가독성 보정)

| 요소 | size | weight | line-height | letter-spacing |
| --- | --- | --- | --- | --- |
| 히어로 H1 | `clamp(36px, 5.4vw, 58px)` | **800** | 1.25 | **-0.035em** |
| 사진 위 타이틀 | `clamp(34px, 4.6vw, 55px)` | **200** | 1.15 | -0.02em |
| 섹션 헤드라인 H2 | `clamp(28px, 4vw, 46px)` | **700** | 1.3 | **-0.03em** |
| 섹션 영문 kicker | 13px | 700 | 1.4 | 0.14em |
| 카드 제목 H3 | 22px | 600 | 34px | -0.01em |
| 통계 숫자 (대) | `clamp(40px, 5.5vw, 60px)` | 600 | 1.0 | normal |
| 통계 숫자 (소) | 33px | 600 | 1.0 | normal |
| 리드 문단 | 18px | 400 | 1.85 | -0.01em |
| 본문 | **16px** | 400 | 1.8 | -0.01em |
| 캡션 | 14px | 400 | 1.7 | -0.01em |
| 버튼 | 17px | 600 | 1 | -0.01em |
| 내비게이션 | 17px | 500 | 25px | -0.5px |

**핵심 변경 3가지**
- 히어로 H1 weight `900 → 800`, 자간 `-0.045em → -0.035em` (Pretendard는 900이 너무 무겁습니다)
- 카드 제목을 19~21px 혼용에서 **22px/600/lh 34px**로 통일 (점프 규격)
- 본문을 15.5~17px 혼용에서 **16px/1.8 하나로 통일**

### 6-3. Thin 웨이트 활용 (점프 시그니처)

사진·다크 배경 위 큰 타이틀에 `font-weight: 200`.
적용 위치: **300시간 타임라인 섹션(다크) H2**, 추후 추가될 사진 밴드 위 문구.

```css
.on-photo-title{
  font-size:clamp(34px,4.6vw,55px);
  font-weight:200; line-height:1.15; color:#fff;
}
```

---

## 7. 일하는학교 적용 — 컴포넌트 규격

### 7-1. 버튼 (전면 교체)

```css
.btn{
  padding:20px 54px;           /* 15px 30px → 20px 54px */
  border-radius:50px;
  font-size:17px; font-weight:600;
  border:0;                    /* ghost 변형 폐지 */
}
.btn--donate { background:var(--donate);     color:#fff; }  /* 후원 전용 */
.btn--brand  { background:var(--brand-deep); color:#fff; }
.btn--neutral{ background:#333A40;           color:#fff; }
.btn--sm     { padding:12px 28px; font-size:15px; }         /* 헤더용 */
```

**폐지**: `.btn--ghost`(테두리만 있는 버튼). 점프에는 없고 후원 CTA의 위계를 흐립니다.
→ 보조 CTA("프로그램 살펴보기")는 `.btn--neutral` 또는 **텍스트 링크 + 화살표**로 강등.

### 7-2. 레이아웃

```css
--wrap:1280px;      /* 1120px → 1280px */
--gutter:32px;      /* 24px → 32px */
--header-h:88px;    /* 70px → 88px */
```

### 7-3. 섹션 헤더 패턴 (신규 컴포넌트)

```
  OUR APPROACH            ← 영문 kicker, 13px/700/자간 0.14em, --brand-ink
                    
  조급한 취업은            ← H2, 46px/700/-0.03em, --ink
  오래 지속되지 못합니다
        │                  ← 세로 구분선 (1px × 40px) ★ 점프 시그니처
  설명 문단 2~3줄          ← 16px/1.8, --ink-2, max-width 62ch
```

```css
.section-head__rule{ width:1px; height:40px; background:var(--line); margin:28px 0; }
.section-head--center .section-head__rule{ margin:28px auto; }
```

### 7-4. 통계 셀 (임팩트 바 교체)

진한 배경 4칸 → **흰 배경 + 얇은 세로 구분선 + 라인 아이콘** 구조로.

```css
.impact__inner{ display:grid; grid-template-columns:repeat(4,1fr); background:#fff; }
.impact__item{ padding:56px 28px; border-right:1px solid var(--line); text-align:center; }
.impact__item:last-child{ border-right:0; }
.impact__icon{ width:44px; height:44px; margin:0 auto 20px; color:var(--brand-deep); } /* stroke 1.5 라인 아이콘 */
.impact__num{ font-size:clamp(40px,5.5vw,60px); font-weight:600; color:var(--ink); letter-spacing:normal; }
.impact__label{ margin-top:10px; font-size:16px; color:var(--ink-2); }
.impact__asof{ margin-top:36px; text-align:center; font-size:14px; color:var(--muted); }
```
→ 하단에 **"2025년 12월 기준"** 기준일 표기 필수 (점프가 쓰는 신뢰 장치).

### 7-5. 사진 밴드 + 떠 있는 카드 (신규)

```
┌──────────────────────────────────┐
│  [ 풀블리드 사진 · 청년 활동 장면 ]  │
│      ┌──────────────────┐        │
└──────│  정기후원 │ 일시후원 │────────┘   ← 탭
       │  월 10,000원 …      │
       │  [ 후원 신청하기 ]    │           ← --donate 버튼
       └──────────────────┘
```

```css
.photo-band{ position:relative; height:clamp(320px,38vw,480px); background:#ccc center/cover; }
.photo-band::after{ content:""; position:absolute; inset:0; background:rgba(17,19,21,.28); }
.float-card{
  position:relative; z-index:2; max-width:520px; margin:-140px auto 0;
  background:#fff; border-radius:20px; padding:36px 40px;
  box-shadow:0 24px 60px rgba(17,19,21,.16);
}
```

### 7-6. 플로팅 후원 장치 (신규)

```css
.donate-fab{
  position:fixed; right:28px; bottom:28px; z-index:60;
  background:var(--donate); color:#fff; border-radius:50px;
  padding:16px 28px; font-size:16px; font-weight:600;
  box-shadow:0 10px 30px rgba(206,69,32,.34);
}
@media (max-width:720px){
  .donate-fab{ left:16px; right:16px; bottom:16px; text-align:center; padding:18px; }
}
```

### 7-7. 라운드 값 통일

| 요소 | 값 |
| --- | --- |
| 버튼 | `50px` |
| 칩·배지 | `999px` |
| 카드 | `20px` (기존 16px → 확대) |
| 큰 카드/플로팅 카드 | `24px` |
| 이미지 밴드 | `0` (풀블리드는 각지게) |

### 7-8. 그림자 (점프는 그림자를 거의 안 씀)

카드는 `1px solid var(--line)` 테두리를 기본으로 하고,
그림자는 **떠 있는 카드와 FAB에만** 사용합니다.

---

## 8. 적용 체크리스트

### A. 색·폰트 — **적용 완료 (2026-09-05)**
- [x] Pretendard 폰트로 교체
- [x] 배경 크림(`#FBF7F1`) → 흰색, 섹션 구분은 `#F5F7F9`
- [x] 그린(`#14563F`)·앰버(`#E0913A`) 전면 폐기 → 하늘색 계열로 교체
- [x] 후원 CTA만 `--donate`(#CE4520)로 전환, 다른 곳 사용 금지
- [x] 다크 섹션을 로고 검정(`#111315`)으로
- [x] 타입 스케일(6-2)대로 크기·웨이트·자간 교체
- [x] 버튼 규격 `20px 54px / 50px / 17px·600` 통일, `.btn--ghost` 폐지
- [x] 컨테이너 1120 → 1280px

### B. 컴포넌트 — **적용 완료 (2026-09-05)**
- [x] 섹션 헤더 7곳에 세로 구분선(`.section-head__rule`) 추가 — 기존 `.mt-32` 중복 여백 제거
- [x] 임팩트 바를 **흰 배경 + 라인아이콘 + 세로 구분선** 구조로 재작업
      (진한 하늘색 밴드 → 흰 배경, 숫자 `--ink` 60px/600, 아이콘 `--brand-deep` stroke 1.5)
- [x] 임팩트 바에 기준일 병기 — "2013~2025년 누적 기준 · 참여청년 512명 자체 조사"
- [x] 카드 라운드 20px, 그림자 제거 (`--shadow-sm:none`) — 그림자는 `.hero__card`·FAB만 유지

> **기준일 문구 주의**: 점프는 "2025년 12월 기준"처럼 특정 시점을 씁니다.
> 일하는학교 원자료(사업소개상세자료)에는 월 단위 기준일이 없어 **"2013~2025년 누적 기준"**으로 적었습니다.
> 정확한 집계 시점이 확인되면 교체하세요.

### C. 구조 — **적용 완료 (2026-09-05)**
- [x] 히어로를 **풀블리드 사진**으로 교체 + 2단 오버레이, 헤더는 사진 위 투명 → 스크롤 시 흰 배경
- [x] 후원 섹션에 **사진 밴드 + 떠 있는 카드**(정기/일시 탭, 110px 겹침)
- [x] STEP 카드 3장 + 그 밖의 사업 카드 3장에 사진
- [x] 우측 하단 후원 FAB / 모바일 하단 고정 바
- [ ] ~~청년 사례 4장에 인물 사진~~ — **의도적으로 넣지 않음** (아래 사유)
- [ ] ~~배경 파스텔 블롭 도형~~ — 히어로가 사진으로 바뀌어 불필요해짐

#### 청년 사례 카드에 인물 사진을 넣지 않은 이유
사례 4건(어린이집 교사 / 게임 개발자 / 사회복지사 / 교육기획자)은 **익명 서술**입니다.
여기에 실제 활동 사진을 붙이면 **사진 속 인물이 그 사례의 당사자로 읽힙니다.**
가정폭력·시설 퇴소·정신질환 등 민감한 내용이 특정 개인과 결부되는 오해가 생기므로,
사진을 쓰려면 ①당사자 본인 사진 + 명시적 동의, 또는 ②"이미지는 사례와 무관" 명기가 필요합니다.
둘 다 확보되기 전까지는 텍스트만 유지합니다.

#### 가이드 대비 의도적 편차 — 히어로 H1 웨이트
6-2 표는 "사진 위 타이틀 = weight 200"으로 정하고 있으나,
히어로 H1은 **700**을 씁니다. 배경이 인물이 많은 단체사진이라 Thin 200 흰 글자는
오버레이를 아무리 올려도 획이 배경에 묻힙니다. 가독성을 우선했습니다.
Thin 200은 **후원 사진 밴드 타이틀**에 적용했습니다(배경이 단순한 야외 사진).

### 사진 에셋

원본: Google Drive 공유 폴더 → `img/`에 리사이즈 후 저장 (총 약 1.4MB)

| 파일 | 원본 | 쓰임 | 크기 |
| --- | --- | --- | --- |
| `hero.jpg` | 시즌1 마침식 단체사진 | 히어로 풀블리드 | 1920×1439 · 436KB |
| `band-donate.jpg` | 강화도 워크숍 야외 단체 | 후원 사진 밴드 | 1920×1440 · 409KB |
| `step1.jpg` | 괜찮은 하루(풍선) | STEP 1 카드 | 900×675 · 88KB |
| `step2.jpg` | 길찾기학교 바리스타 | STEP 2 카드 | 900×1200 · 148KB |
| `step3.jpg` | 꽃길 프로젝트 실무 | STEP 3 카드 | 900×675 · 66KB |
| `biz-cafe.jpg` | 카페 일경험 | 카페 그런날 카드 | 900×675 · 98KB |
| `biz-somoim.jpg` | 글쓰기 모임 | 소모임 카드 | 900×675 · 104KB |
| `biz-research.jpg` | 성과 공유 발표회 | 연구·조사 카드 | 900×506 · 80KB |

**사진 선택 기준**: 얼굴 식별이 덜 되는 컷을 우선했습니다.
`step1`은 풍선으로 얼굴이 가려지고, `step3`는 손과 작업물만 나옵니다.
`hero`·`band-donate`는 단체사진이라 얼굴이 보입니다 — **공개 웹 게시에 대한 초상권 동의 확인 필요**.

**교체 방법**: `img/` 안의 파일을 같은 이름으로 덮어쓰면 됩니다.
카드 사진은 `aspect-ratio:16/10`으로 잘리므로 가로가 긴 원본이 좋습니다.

> **주의 — `<img>`의 width/height 속성**: 이 속성은 CSS `aspect-ratio`를 무력화합니다
> (속성이 presentational hint로 `height`를 고정시킴). 반드시 `height:auto`를 함께 지정하세요.
> 실제로 이 버그로 카드 사진이 386×675로 세로로 늘어났던 적이 있습니다.

#### 후원 FAB 노출 규칙
본 CTA와 경쟁하지 않도록 구간을 나눕니다.

| 스크롤 위치 | FAB |
| --- | --- |
| 히어로 안 (`scrollY < 0.7 × 뷰포트`) | 숨김 — 히어로 CTA가 이미 있음 |
| 본문 전체 | **노출** |
| 참여 섹션(`#join`)이 화면에 들어옴 | 숨김 — 본 CTA에 양보 |
| 참여 섹션을 지나 푸터 | 노출 |

모바일(≤720px)에서는 우측 원형 대신 **하단 고정 바** 전체폭으로 전환됩니다.

### D. 하지 않을 것
- [ ] ~~fullPage.js 스크롤 하이재킹~~ — 일반 스크롤 유지
- [ ] ~~점프의 레드(#F23838) 후원색~~ — 버밀리언 사용
- [ ] ~~본문 14px~~ — 16px 유지

---

## 부록 — 토큰 블록 (index.html에 반영된 실제 값)

```css
:root{
  /* ---- color : 로고 하늘색 #68C9F3 / 로고 검정 #111315 기준 ---- */
  --brand:#68C9F3;           /* 면·도형·밑줄·불릿. 글자로 쓰지 말 것 */
  --brand-deep:#1595CC;      /* 버튼·아이콘·큰 숫자 (24px+) */
  --brand-ink:#0E6E99;       /* 작은 강조 텍스트·링크 (AA 5.7:1) */
  --brand-tint:#EAF7FE;      /* 옅은 강조 배경 */

  --donate:#CE4520;          /* 후원 버튼 전용 — 다른 곳 사용 금지 */
  --donate-hover:#B33A19;

  --bg:#FFFFFF;
  --surface:#F5F7F9;
  --dark:#111315;

  --ink:#111315;             /* 제목 */
  --ink-2:#4A5560;           /* 본문 (7.5:1) */
  --muted:#6B7681;           /* 캡션 (4.7:1) */
  --line:#E4E9ED;
  --line-soft:#EEF2F5;

  /* ---- type ---- */
  --font:"Pretendard Variable",Pretendard,-apple-system,BlinkMacSystemFont,
         system-ui,"Apple SD Gothic Neo","Noto Sans KR","Malgun Gothic",sans-serif;

  /* ---- layout ---- */
  --wrap:1280px;
  --gutter:32px;
  --header-h:88px;

  /* ---- radius ---- */
  --r-btn:50px; --r-card:20px; --r-card-lg:24px; --r-pill:999px;

  /* ---- shadow : 떠 있는 요소에만 ---- */
  --shadow-float:0 24px 60px rgba(17,19,21,.16);
  --shadow-fab:0 10px 30px rgba(206,69,32,.34);

  /* ---- rhythm ---- */
  --sec-pad:clamp(72px,9vw,120px);
}
```

---

### 실측 방법 및 한계
- 2026-09-05 기준 jumpsp.org 메인 / `/donate/` / `/about-jump/social-value-impact/` 세 페이지를
  브라우저에서 열어 `getComputedStyle`로 색·타이포·버튼·컨테이너 값을 집계했습니다.
- 사이트가 fullPage.js 기반이라 스크롤이 잠겨 있어, 전체 섹션 확인을 위해 DOM을 임시로 풀었습니다.
  이 과정에서 레이아웃이 일부 깨졌으므로 **섹션 여백(padding) 값은 실측이 아닌 권장값**입니다.
- 일하는학교 로고 색은 같은 작업 폴더의 `Index.html`(Apps Script 프로젝트) 주석에 기재된 값을 사용했습니다.
  **원본 로고 파일을 직접 확인한 것은 아니므로**, 로고 이미지가 확보되면 재검증이 필요합니다.
- 점프의 사진·아이콘 등 저작물은 참고만 했으며 어떤 에셋도 가져오지 않았습니다.
